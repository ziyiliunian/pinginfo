# 修改记录 (CHANGELOG)

本文件记录 PingInfo 各版本的主要变更。

## [1.4.0] - 2026-08-31

### 表格交互
- 点击监控列或序号列可选中整行，并支持从这两列拖动调整目标顺序
- 目标序号在创建时固定，不随排序或拖动发生变化
- 新增目标默认不勾选，点击“监控”表头可全选或全部取消
- 主表格移除 MAC 地址列，底部仅显示目标数和启用数

### 查询与界面
- 工具菜单的 MAC 查询仅处理选中主机，并以带框线的结果弹窗逐项展示
- 目标详情改为带边框的字段列表，提升空值和字段之间的辨识度
- 帮助中的关于窗口增加项目 Git 地址、作者和联系方式

## [1.3.0] - 2026-08-31

### 界面模型
- 主表格由 `QTableWidget` 重构为 `QTableView`、`QAbstractTableModel` 与 `QSortFilterProxyModel`
- Ping、DNS、MAC、启停和勾选操作改为按目标或列局部刷新，不再重建整张表格
- 排序与底层目标列表解耦，排序后的删除、启停、查询和导出统一按模型索引映射
- 数值列按原始数值排序，缺失值在升序和降序中均保持置底

### 窗口与详情
- 程序启动时自动显示添加 Ping 目标弹窗
- “从新添加”和“从文件重新载入”改为独立新窗口，不修改原窗口的目标与监控状态
- 表格右键新增目标详情，可查看主机名、IP、真实响应地址、成功/失败次数、时延、TTL、MAC、错误和历史结果

## [1.2.0] - 2026-08-31

### 界面与交互
- 加深整行选中背景，使用 2px 深色连续外围框线，不绘制内部单元格框线
- 空白区域右键仅显示可执行的“添加目标”
- 刷新和排序后按目标对象身份恢复选择，避免后续删除/启停/导出作用错对象
- 非连续单元格复制保持矩形 TSV 列位置
- 修复禁用目标重新启用后文字仍保持灰色
- 序号列改为数值排序
- 修复添加窗口重复注册 Ctrl+V，以及 ICMP 模式静默忽略端口
- 拖放只接受支持内容并明确反馈成功/失败

### 线程与网络
- 修复监控期间删除目标后线程仍轮询旧列表、后续新增不生效
- 停止监控和关闭窗口改为非阻塞信号/定时器流程，避免 GUI 假死
- 大量目标按并发数分波 Ping，每波检查停止请求
- DNS/MAC 后台任务增加整体 deadline 并取消未开始 future
- Windows MAC 查询改用 `arp -a`，Windows 包大小 56 显式传入

### 导出与打包
- TXT/CSV/HTML/XML 使用同目录临时文件原子替换，失败不破坏旧报告
- XML 导出过滤 XML 1.0 禁止控制字符
- `iputils-ping` 提升为运行时 Depends
- 取消安装阶段生成未跟踪 pyc
- 构建版本从 `src.__version__` 单一读取，参数仅用于一致性校验
- 构建复用静态 PNG，不再要求构建机安装 PyQt5
- 地址文件读取兼容 UTF-8 BOM

## [1.1.6] - 2026-08-31

### 修复
- `resolve_to_ipv4(timeout)` 改为标准库子进程解析，DNS 超时可真实终止，不增加外部依赖
- IPv6 被误当域名：地址识别统一支持 IPv4/IPv6
- 添加、文件载入和拖放支持 `[IPv6]:port`，裸 IPv6 不再误拆端口
- 关闭窗口时等待 DNS/MAC/监控线程结束，避免运行中的 QThread 被销毁
- 平均 RTT 仅使用有效 RTT 样本作为分母
- TCP 连接被拒时的 RTT 保留并传递到界面统计
- CSV 导出防止 `= + - @` 等用户输入触发表格公式
- 表格刷新后保持用户当前排序列及升降序

### 稳定性
- 监控每轮使用目标列表快照
- 后台线程完成后安全移出跟踪列表并 `deleteLater`

### 分析报告结论
- 报告 #3 所称“list 变更触发 RuntimeError”不成立，但使用批次快照更稳健
- #6 序号为当前列表展示序号，未承诺跨会话持久化，不作为 bug 修改
- #8 RTT 历史明确限制最近 20 条，是有界缓存设计，不是无限增长

## [1.1.5] - 2026-08-26

### 优化
- 监控列复选框在单元格中真正居中显示
- 去除表格外围框线、单元格内部框线和点击焦点框
- 表头由蓝色改为浅灰色中性样式
- 点击监控列中心复选框时切换勾选；点击外围空白区域时不改变勾选并高亮整行

## [1.1.4] - 2026-08-26

### 新增
- 表格支持单元格级选择，解析 IP、MAC 地址等内容可独立选中
- 新增 `Ctrl+C` 和右键“复制单元格内容”，多选单元格按 TSV 格式复制

### 优化
- 选择列改名为“监控”并加宽，复选框居中且增加用途提示
- 优化表格、表头、隔行颜色、行高及选中状态视觉效果
- `dist/` 改为仅保留并提交最近两个正式 deb 版本

## [1.1.3] - 2026-08-25

### 修复
- 域名解析地址不显示：新增输入规范化（去除 `http://` 等协议前缀、URL 路径、尾部斜杠），带 URL 格式的域名也能正确解析显示
- MAC 查询无反应：
  - 域名目标先解析为 IPv4 再查询（原先把域名直接传给 `ip neigh` 导致静默失败）
  - 查询前先 ping 一次目标，填充 ARP 邻居缓存
  - 右键点击条目时自动选中该条目，"查询 MAC 地址"优先作用于选中条目
  - 无结果时明确提示（仅同一子网且可达的目标可查询）

## [1.1.2] - 2026-08-25

### 修复（bug 清单 11 项）
- [崩溃] 兼容中文 Windows ping 输出（`时间=10ms`/`时间<1ms`），RTT 为 None 时不再抛 TypeError
- [数据错乱] 表格排序后删除/启停/勾选/导出选中行按目标对象映射，不再操作到错误目标
- [内存/卡死] CIDR 展开增加 65535 数量上限，防止 `10.0.0.0/8` 等耗尽内存
- [静默丢目标] 添加去重键加入 TCP 端口，`host:80` 与 `host:443` 可同时存在
- [界面卡死] 域名解析移至后台线程（ResolveWorker），带 30 秒整体超时
- [界面卡死] MAC 地址查询移至后台线程（MacWorker）
- [生命周期] 停止监控时断开旧线程信号，超时未结束的线程自动销毁
- [输出注入] HTML 导出使用 `html.escape()` 转义特殊字符
- [图标/打包] 修正图标回退路径为 `/usr/share/icons/...`；Installed-Size 统计 /opt 与 /usr
- [健壮性] tcp_ping 关闭后置 None 避免双 close；精简冗余异常捕获
- [语义] packet_size=0 视为系统默认，不再传 `-s 0`/`-l 0`

### 其他
- 统一全项目版本号；control Homepage 修正为真实仓库地址

## [1.1.1] - 2026-08-21

### 修复
- 添加目标时长 IP/域名自动换行问题（输入框改为不换行 + 水平滚动条）

### 优化
- 大量目标性能：添加去重 O(n²)→O(n)；单元格仅在内容变化时更新；复用复选框 item（500 目标单批刷新约 100ms+ → 19ms）

## [1.1.0] - 2026-08-18

### 变更
- 统一版本号管理：`__init__.py` 为单一数据源，关于对话框动态读取
- 清理旧版本 deb 产物

## [1.0.2] - 2026-08-18

### 修复
- 任务栏图标：显式设置窗口图标、`setDesktopFileName` 关联 desktop、添加 StartupWMClass
- 修正 postinst 安装路径（/usr/share → /opt）
- 修正"帮助 > 关于"中的版本号显示

## [1.0.1] - 2026-08-17

### 修复
- deb 架构字段改为 `all`，支持 arm64/armhf/amd64 等多平台安装
- 数值列（响应时间/丢包率等）按真实数值排序，缺失值排末尾

### 变更
- 程序安装路径调整为 `/opt/pinginfo`
- 重新设计产品 logo（雷达探测 + 目标节点含义）

## [1.0.0] - 2026-08-17

### 首个 deb 发布版本
- 批量 Ping 与实时监控（ICMP/TCP、IPv4/IPv6）
- 响应时间/丢包率/延迟统计、TTL、MAC 地址查询
- 域名自动解析显示 IPv4 地址
- 导出 TXT / CSV / HTML / XML
- 并发线程默认 500、最大 2000
- deb 打包（安装到 /opt，桌面菜单集成）
